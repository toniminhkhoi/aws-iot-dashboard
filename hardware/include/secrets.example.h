#pragma once

// Copy this file to secrets.h, then replace the placeholder values.
// secrets.h is ignored by Git so credentials are not committed.
constexpr char WIFI_SSID[] = "YOUR_WIFI_SSID";
constexpr char WIFI_PASSWORD[] = "YOUR_WIFI_PASSWORD";
constexpr char API_BASE_URL[] = "http://YOUR_EC2_HOST:8000";
constexpr char DEVICE_ID[] = "room_01";
